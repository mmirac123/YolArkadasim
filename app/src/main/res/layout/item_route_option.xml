<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="6dp"
    android:layout_marginHorizontal="4dp"
    android:clickable="true"
    android:focusable="true"
    app:cardBackgroundColor="@color/surface_card_elevated"
    app:cardCornerRadius="16dp"
    app:cardElevation="3dp"
    app:rippleColor="@color/accent_blue_ripple">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="20dp"
        android:paddingVertical="18dp"
        android:minHeight="80dp">

        <!-- Route number badge or stop index -->
        <TextView
            android:id="@+id/textOptionBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minWidth="56dp"
            android:gravity="center"
            android:textColor="@color/white"
            android:textSize="22sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-black"
            android:background="@drawable/bg_badge_rounded"
            android:paddingHorizontal="14dp"
            android:paddingVertical="8dp"
            tools:text="521" />

        <!-- Route name / Stop name -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="16dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/textOptionTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/text_primary"
                android:textSize="20sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif"
                tools:text="Fatih (GOP) – Ümitköy Metro" />

            <TextView
                android:id="@+id/textOptionSubtitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:textColor="@color/text_secondary"
                android:textSize="16sp"
                android:fontFamily="sans-serif"
                tools:text="Fatih → Ümitköy" />
        </LinearLayout>

        <!-- Favorite Icon (Star) -->
        <ImageButton
            android:id="@+id/btnFavorite"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Favoriye Ekle"
            android:padding="10dp"
            android:scaleType="centerInside"
            android:src="@drawable/ic_star_outline"
            android:layout_marginEnd="4dp" />

        <!-- Audio Preview Icon -->
        <ImageButton
            android:id="@+id/btnAudioPreview"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/cd_audio_preview"
            android:padding="8dp"
            android:scaleType="centerInside"
            android:src="@android:drawable/ic_lock_silent_mode_off"
            app:tint="@color/accent_blue"
            android:visibility="gone"
            tools:visibility="visible" />

        <!-- Arrow indicator -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="›"
            android:textColor="@color/text_secondary"
            android:textSize="32sp"
            android:gravity="center"
            android:paddingStart="8dp"
            android:importantForAccessibility="no" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
